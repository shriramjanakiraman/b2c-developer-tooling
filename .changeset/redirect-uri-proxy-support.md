---
'@salesforce/b2c-tooling-sdk': minor
---

Add `SFCC_REDIRECT_URI` environment variable to support implicit OAuth flow behind a proxy. Set this to your proxy URL when `localhost:8080` isn't directly reachable by the browser.
